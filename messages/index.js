const AuthenticationMessages = require('./authentication');
const ValidationMessages = require('./validation');
const CardMessages = require('./card');

module.exports = {
  AuthenticationMessages,
  ValidationMessages,
  CardMessages,
};
